// Auto-generated by generate_struct_convert.py - DO NOT EDIT
// Generated: 2026-04-28T22:25:30.320713


#include "struct_convert.h"

int __kernel_fsid_t_host_to_wasm32(const struct host64___kernel_fsid_t *host, struct wasm32___kernel_fsid_t *wasm) {
    for (int i = 0; i < 2; i++) wasm->val[i] = host->val[i];
    return 0;
}

void __kernel_fsid_t_wasm32_to_host(const struct wasm32___kernel_fsid_t *wasm, struct host64___kernel_fsid_t *host) {
    for (int i = 0; i < 2; i++) host->val[i] = wasm->val[i];
}

int __kernel_itimerspec_host_to_wasm32(const struct host64___kernel_itimerspec *host, struct wasm32___kernel_itimerspec *wasm) {
    if (__kernel_timespec_host_to_wasm32(&host->it_interval, &wasm->it_interval) != 0) return -1;
    if (__kernel_timespec_host_to_wasm32(&host->it_value, &wasm->it_value) != 0) return -1;
    return 0;
}

void __kernel_itimerspec_wasm32_to_host(const struct wasm32___kernel_itimerspec *wasm, struct host64___kernel_itimerspec *host) {
    __kernel_timespec_wasm32_to_host(&wasm->it_interval, &host->it_interval);
    __kernel_timespec_wasm32_to_host(&wasm->it_value, &host->it_value);
}

int __kernel_old_itimerval_host_to_wasm32(const struct host64___kernel_old_itimerval *host, struct wasm32___kernel_old_itimerval *wasm) {
    if (__kernel_old_timeval_host_to_wasm32(&host->it_interval, &wasm->it_interval) != 0) return -1;
    if (__kernel_old_timeval_host_to_wasm32(&host->it_value, &wasm->it_value) != 0) return -1;
    return 0;
}

void __kernel_old_itimerval_wasm32_to_host(const struct wasm32___kernel_old_itimerval *wasm, struct host64___kernel_old_itimerval *host) {
    __kernel_old_timeval_wasm32_to_host(&wasm->it_interval, &host->it_interval);
    __kernel_old_timeval_wasm32_to_host(&wasm->it_value, &host->it_value);
}

int __kernel_old_timeval_host_to_wasm32(const struct host64___kernel_old_timeval *host, struct wasm32___kernel_old_timeval *wasm) {
    if (host->tv_sec > 2147483647LL || host->tv_sec < -2147483648LL) return -1;
    wasm->tv_sec = (int32_t)host->tv_sec;
    if (host->tv_usec > 2147483647LL || host->tv_usec < -2147483648LL) return -1;
    wasm->tv_usec = (int32_t)host->tv_usec;
    return 0;
}

void __kernel_old_timeval_wasm32_to_host(const struct wasm32___kernel_old_timeval *wasm, struct host64___kernel_old_timeval *host) {
    host->tv_sec = (int64_t)(int32_t)wasm->tv_sec;
    host->tv_usec = (int64_t)(int32_t)wasm->tv_usec;
}

int __kernel_timespec_host_to_wasm32(const struct host64___kernel_timespec *host, struct wasm32___kernel_timespec *wasm) {
    wasm->tv_sec = host->tv_sec;
    wasm->tv_nsec = host->tv_nsec;
    return 0;
}

void __kernel_timespec_wasm32_to_host(const struct wasm32___kernel_timespec *wasm, struct host64___kernel_timespec *host) {
    host->tv_sec = wasm->tv_sec;
    host->tv_nsec = wasm->tv_nsec;
}

int __kernel_timex_host_to_wasm32(const struct host64___kernel_timex *host, struct wasm32___kernel_timex *wasm) {
    wasm->modes = host->modes;
    wasm->offset = host->offset;
    wasm->freq = host->freq;
    wasm->maxerror = host->maxerror;
    wasm->esterror = host->esterror;
    wasm->status = host->status;
    wasm->constant = host->constant;
    wasm->precision = host->precision;
    wasm->tolerance = host->tolerance;
    if (__kernel_timex_timeval_host_to_wasm32(&host->time, &wasm->time) != 0) return -1;
    wasm->tick = host->tick;
    wasm->ppsfreq = host->ppsfreq;
    wasm->jitter = host->jitter;
    wasm->shift = host->shift;
    wasm->stabil = host->stabil;
    wasm->jitcnt = host->jitcnt;
    wasm->calcnt = host->calcnt;
    wasm->errcnt = host->errcnt;
    wasm->stbcnt = host->stbcnt;
    wasm->tai = host->tai;
    return 0;
}

void __kernel_timex_wasm32_to_host(const struct wasm32___kernel_timex *wasm, struct host64___kernel_timex *host) {
    host->modes = wasm->modes;
    host->offset = wasm->offset;
    host->freq = wasm->freq;
    host->maxerror = wasm->maxerror;
    host->esterror = wasm->esterror;
    host->status = wasm->status;
    host->constant = wasm->constant;
    host->precision = wasm->precision;
    host->tolerance = wasm->tolerance;
    __kernel_timex_timeval_wasm32_to_host(&wasm->time, &host->time);
    host->tick = wasm->tick;
    host->ppsfreq = wasm->ppsfreq;
    host->jitter = wasm->jitter;
    host->shift = wasm->shift;
    host->stabil = wasm->stabil;
    host->jitcnt = wasm->jitcnt;
    host->calcnt = wasm->calcnt;
    host->errcnt = wasm->errcnt;
    host->stbcnt = wasm->stbcnt;
    host->tai = wasm->tai;
}

int __kernel_timex_timeval_host_to_wasm32(const struct host64___kernel_timex_timeval *host, struct wasm32___kernel_timex_timeval *wasm) {
    wasm->tv_sec = host->tv_sec;
    wasm->tv_usec = host->tv_usec;
    return 0;
}

void __kernel_timex_timeval_wasm32_to_host(const struct wasm32___kernel_timex_timeval *wasm, struct host64___kernel_timex_timeval *host) {
    host->tv_sec = wasm->tv_sec;
    host->tv_usec = wasm->tv_usec;
}

int __old_kernel_stat_host_to_wasm32(const struct host64___old_kernel_stat *host, struct wasm32___old_kernel_stat *wasm) {
    wasm->st_dev = host->st_dev;
    wasm->st_ino = host->st_ino;
    wasm->st_mode = host->st_mode;
    wasm->st_nlink = host->st_nlink;
    wasm->st_uid = host->st_uid;
    wasm->st_gid = host->st_gid;
    wasm->st_rdev = host->st_rdev;
    wasm->st_size = host->st_size;
    wasm->st_atime = host->st_atime;
    wasm->st_mtime = host->st_mtime;
    wasm->st_ctime = host->st_ctime;
    return 0;
}

void __old_kernel_stat_wasm32_to_host(const struct wasm32___old_kernel_stat *wasm, struct host64___old_kernel_stat *host) {
    host->st_dev = wasm->st_dev;
    host->st_ino = wasm->st_ino;
    host->st_mode = wasm->st_mode;
    host->st_nlink = wasm->st_nlink;
    host->st_uid = wasm->st_uid;
    host->st_gid = wasm->st_gid;
    host->st_rdev = wasm->st_rdev;
    host->st_size = wasm->st_size;
    host->st_atime = wasm->st_atime;
    host->st_mtime = wasm->st_mtime;
    host->st_ctime = wasm->st_ctime;
}

int __user_cap_data_struct_host_to_wasm32(const struct host64___user_cap_data_struct *host, struct wasm32___user_cap_data_struct *wasm) {
    wasm->effective = host->effective;
    wasm->permitted = host->permitted;
    wasm->inheritable = host->inheritable;
    return 0;
}

void __user_cap_data_struct_wasm32_to_host(const struct wasm32___user_cap_data_struct *wasm, struct host64___user_cap_data_struct *host) {
    host->effective = wasm->effective;
    host->permitted = wasm->permitted;
    host->inheritable = wasm->inheritable;
}

int __user_cap_header_struct_host_to_wasm32(const struct host64___user_cap_header_struct *host, struct wasm32___user_cap_header_struct *wasm) {
    wasm->version = host->version;
    wasm->pid = host->pid;
    return 0;
}

void __user_cap_header_struct_wasm32_to_host(const struct wasm32___user_cap_header_struct *wasm, struct host64___user_cap_header_struct *host) {
    host->version = wasm->version;
    host->pid = wasm->pid;
}

int cachestat_host_to_wasm32(const struct host64_cachestat *host, struct wasm32_cachestat *wasm) {
    wasm->nr_cache = host->nr_cache;
    wasm->nr_dirty = host->nr_dirty;
    wasm->nr_writeback = host->nr_writeback;
    wasm->nr_evicted = host->nr_evicted;
    wasm->nr_recently_evicted = host->nr_recently_evicted;
    return 0;
}

void cachestat_wasm32_to_host(const struct wasm32_cachestat *wasm, struct host64_cachestat *host) {
    host->nr_cache = wasm->nr_cache;
    host->nr_dirty = wasm->nr_dirty;
    host->nr_writeback = wasm->nr_writeback;
    host->nr_evicted = wasm->nr_evicted;
    host->nr_recently_evicted = wasm->nr_recently_evicted;
}

int cachestat_range_host_to_wasm32(const struct host64_cachestat_range *host, struct wasm32_cachestat_range *wasm) {
    wasm->off = host->off;
    wasm->len = host->len;
    return 0;
}

void cachestat_range_wasm32_to_host(const struct wasm32_cachestat_range *wasm, struct host64_cachestat_range *host) {
    host->off = wasm->off;
    host->len = wasm->len;
}

int clone_args_host_to_wasm32(const struct host64_clone_args *host, struct wasm32_clone_args *wasm) {
    wasm->flags = host->flags;
    wasm->pidfd = host->pidfd;
    wasm->child_tid = host->child_tid;
    wasm->parent_tid = host->parent_tid;
    wasm->exit_signal = host->exit_signal;
    wasm->stack = host->stack;
    wasm->stack_size = host->stack_size;
    wasm->tls = host->tls;
    wasm->set_tid = host->set_tid;
    wasm->set_tid_size = host->set_tid_size;
    wasm->cgroup = host->cgroup;
    return 0;
}

void clone_args_wasm32_to_host(const struct wasm32_clone_args *wasm, struct host64_clone_args *host) {
    host->flags = wasm->flags;
    host->pidfd = wasm->pidfd;
    host->child_tid = wasm->child_tid;
    host->parent_tid = wasm->parent_tid;
    host->exit_signal = wasm->exit_signal;
    host->stack = wasm->stack;
    host->stack_size = wasm->stack_size;
    host->tls = wasm->tls;
    host->set_tid = wasm->set_tid;
    host->set_tid_size = wasm->set_tid_size;
    host->cgroup = wasm->cgroup;
}

int epoll_event_host_to_wasm32(const struct host64_epoll_event *host, struct wasm32_epoll_event *wasm) {
    wasm->events = host->events;
    wasm->data = host->data;
    return 0;
}

void epoll_event_wasm32_to_host(const struct wasm32_epoll_event *wasm, struct host64_epoll_event *host) {
    host->events = wasm->events;
    host->data = wasm->data;
}

int futex_waitv_host_to_wasm32(const struct host64_futex_waitv *host, struct wasm32_futex_waitv *wasm) {
    wasm->val = host->val;
    wasm->uaddr = host->uaddr;
    wasm->flags = host->flags;
    return 0;
}

void futex_waitv_wasm32_to_host(const struct wasm32_futex_waitv *wasm, struct host64_futex_waitv *host) {
    host->val = wasm->val;
    host->uaddr = wasm->uaddr;
    host->flags = wasm->flags;
}

int io_cqring_offsets_host_to_wasm32(const struct host64_io_cqring_offsets *host, struct wasm32_io_cqring_offsets *wasm) {
    wasm->head = host->head;
    wasm->tail = host->tail;
    wasm->ring_mask = host->ring_mask;
    wasm->ring_entries = host->ring_entries;
    wasm->overflow = host->overflow;
    wasm->cqes = host->cqes;
    wasm->flags = host->flags;
    wasm->resv1 = host->resv1;
    wasm->user_addr = host->user_addr;
    return 0;
}

void io_cqring_offsets_wasm32_to_host(const struct wasm32_io_cqring_offsets *wasm, struct host64_io_cqring_offsets *host) {
    host->head = wasm->head;
    host->tail = wasm->tail;
    host->ring_mask = wasm->ring_mask;
    host->ring_entries = wasm->ring_entries;
    host->overflow = wasm->overflow;
    host->cqes = wasm->cqes;
    host->flags = wasm->flags;
    host->resv1 = wasm->resv1;
    host->user_addr = wasm->user_addr;
}

int io_event_host_to_wasm32(const struct host64_io_event *host, struct wasm32_io_event *wasm) {
    wasm->data = host->data;
    wasm->obj = host->obj;
    wasm->res = host->res;
    wasm->res2 = host->res2;
    return 0;
}

void io_event_wasm32_to_host(const struct wasm32_io_event *wasm, struct host64_io_event *host) {
    host->data = wasm->data;
    host->obj = wasm->obj;
    host->res = wasm->res;
    host->res2 = wasm->res2;
}

int io_sqring_offsets_host_to_wasm32(const struct host64_io_sqring_offsets *host, struct wasm32_io_sqring_offsets *wasm) {
    wasm->head = host->head;
    wasm->tail = host->tail;
    wasm->ring_mask = host->ring_mask;
    wasm->ring_entries = host->ring_entries;
    wasm->flags = host->flags;
    wasm->dropped = host->dropped;
    wasm->array = host->array;
    wasm->resv1 = host->resv1;
    wasm->user_addr = host->user_addr;
    return 0;
}

void io_sqring_offsets_wasm32_to_host(const struct wasm32_io_sqring_offsets *wasm, struct host64_io_sqring_offsets *host) {
    host->head = wasm->head;
    host->tail = wasm->tail;
    host->ring_mask = wasm->ring_mask;
    host->ring_entries = wasm->ring_entries;
    host->flags = wasm->flags;
    host->dropped = wasm->dropped;
    host->array = wasm->array;
    host->resv1 = wasm->resv1;
    host->user_addr = wasm->user_addr;
}

int io_uring_params_host_to_wasm32(const struct host64_io_uring_params *host, struct wasm32_io_uring_params *wasm) {
    wasm->sq_entries = host->sq_entries;
    wasm->cq_entries = host->cq_entries;
    wasm->flags = host->flags;
    wasm->sq_thread_cpu = host->sq_thread_cpu;
    wasm->sq_thread_idle = host->sq_thread_idle;
    wasm->features = host->features;
    wasm->wq_fd = host->wq_fd;
    for (int i = 0; i < 3; i++) wasm->resv[i] = host->resv[i];
    if (io_sqring_offsets_host_to_wasm32(&host->sq_off, &wasm->sq_off) != 0) return -1;
    if (io_cqring_offsets_host_to_wasm32(&host->cq_off, &wasm->cq_off) != 0) return -1;
    return 0;
}

void io_uring_params_wasm32_to_host(const struct wasm32_io_uring_params *wasm, struct host64_io_uring_params *host) {
    host->sq_entries = wasm->sq_entries;
    host->cq_entries = wasm->cq_entries;
    host->flags = wasm->flags;
    host->sq_thread_cpu = wasm->sq_thread_cpu;
    host->sq_thread_idle = wasm->sq_thread_idle;
    host->features = wasm->features;
    host->wq_fd = wasm->wq_fd;
    for (int i = 0; i < 3; i++) host->resv[i] = wasm->resv[i];
    io_sqring_offsets_wasm32_to_host(&wasm->sq_off, &host->sq_off);
    io_cqring_offsets_wasm32_to_host(&wasm->cq_off, &host->cq_off);
}

int iocb_host_to_wasm32(const struct host64_iocb *host, struct wasm32_iocb *wasm) {
    wasm->aio_data = host->aio_data;
    wasm->aio_key = host->aio_key;
    wasm->aio_rw_flags = host->aio_rw_flags;
    wasm->aio_lio_opcode = host->aio_lio_opcode;
    wasm->aio_reqprio = host->aio_reqprio;
    wasm->aio_fildes = host->aio_fildes;
    wasm->aio_buf = host->aio_buf;
    wasm->aio_nbytes = host->aio_nbytes;
    wasm->aio_offset = host->aio_offset;
    wasm->aio_reserved2 = host->aio_reserved2;
    wasm->aio_flags = host->aio_flags;
    wasm->aio_resfd = host->aio_resfd;
    return 0;
}

void iocb_wasm32_to_host(const struct wasm32_iocb *wasm, struct host64_iocb *host) {
    host->aio_data = wasm->aio_data;
    host->aio_key = wasm->aio_key;
    host->aio_rw_flags = wasm->aio_rw_flags;
    host->aio_lio_opcode = wasm->aio_lio_opcode;
    host->aio_reqprio = wasm->aio_reqprio;
    host->aio_fildes = wasm->aio_fildes;
    host->aio_buf = wasm->aio_buf;
    host->aio_nbytes = wasm->aio_nbytes;
    host->aio_offset = wasm->aio_offset;
    host->aio_reserved2 = wasm->aio_reserved2;
    host->aio_flags = wasm->aio_flags;
    host->aio_resfd = wasm->aio_resfd;
}

int iovec_host_to_wasm32(const struct host64_iovec *host, struct wasm32_iovec *wasm, uint8_t *mem_base) {
    wasm->iov_base = host->iov_base ? (uint32_t)((uint8_t *)host->iov_base - mem_base) : 0;
    if ((uint64_t)host->iov_len > 4294967295ULL) return -1;
    wasm->iov_len = (uint32_t)host->iov_len;
    return 0;
}

void iovec_wasm32_to_host(const struct wasm32_iovec *wasm, struct host64_iovec *host, uint8_t *mem_base) {
    host->iov_base = wasm->iov_base ? (uint64_t)(uintptr_t)(mem_base + wasm->iov_base) : 0;
    host->iov_len = wasm->iov_len;
}

int ipc_perm_host_to_wasm32(const struct host64_ipc_perm *host, struct wasm32_ipc_perm *wasm) {
    wasm->key = host->key;
    if ((uint64_t)host->uid > 65535ULL) return -1;
    wasm->uid = (uint16_t)host->uid;
    if ((uint64_t)host->gid > 65535ULL) return -1;
    wasm->gid = (uint16_t)host->gid;
    if ((uint64_t)host->cuid > 65535ULL) return -1;
    wasm->cuid = (uint16_t)host->cuid;
    if ((uint64_t)host->cgid > 65535ULL) return -1;
    wasm->cgid = (uint16_t)host->cgid;
    if ((uint64_t)host->mode > 65535ULL) return -1;
    wasm->mode = (uint16_t)host->mode;
    wasm->seq = host->seq;
    return 0;
}

void ipc_perm_wasm32_to_host(const struct wasm32_ipc_perm *wasm, struct host64_ipc_perm *host) {
    host->key = wasm->key;
    host->uid = wasm->uid;
    host->gid = wasm->gid;
    host->cuid = wasm->cuid;
    host->cgid = wasm->cgid;
    host->mode = wasm->mode;
    host->seq = wasm->seq;
}

int kexec_segment_host_to_wasm32(const struct host64_kexec_segment *host, struct wasm32_kexec_segment *wasm, uint8_t *mem_base) {
    wasm->buf = host->buf ? (uint32_t)((uint8_t *)host->buf - mem_base) : 0;
    if ((uint64_t)host->bufsz > 4294967295ULL) return -1;
    wasm->bufsz = (uint32_t)host->bufsz;
    wasm->mem = host->mem ? (uint32_t)((uint8_t *)host->mem - mem_base) : 0;
    if ((uint64_t)host->memsz > 4294967295ULL) return -1;
    wasm->memsz = (uint32_t)host->memsz;
    return 0;
}

void kexec_segment_wasm32_to_host(const struct wasm32_kexec_segment *wasm, struct host64_kexec_segment *host, uint8_t *mem_base) {
    host->buf = wasm->buf ? (uint64_t)(uintptr_t)(mem_base + wasm->buf) : 0;
    host->bufsz = wasm->bufsz;
    host->mem = wasm->mem ? (uint64_t)(uintptr_t)(mem_base + wasm->mem) : 0;
    host->memsz = wasm->memsz;
}

int landlock_ruleset_attr_host_to_wasm32(const struct host64_landlock_ruleset_attr *host, struct wasm32_landlock_ruleset_attr *wasm) {
    wasm->handled_access_fs = host->handled_access_fs;
    wasm->handled_access_net = host->handled_access_net;
    wasm->scoped = host->scoped;
    return 0;
}

void landlock_ruleset_attr_wasm32_to_host(const struct wasm32_landlock_ruleset_attr *wasm, struct host64_landlock_ruleset_attr *host) {
    host->handled_access_fs = wasm->handled_access_fs;
    host->handled_access_net = wasm->handled_access_net;
    host->scoped = wasm->scoped;
}

int lsm_ctx_host_to_wasm32(const struct host64_lsm_ctx *host, struct wasm32_lsm_ctx *wasm) {
    wasm->id = host->id;
    wasm->flags = host->flags;
    wasm->len = host->len;
    wasm->ctx_len = host->ctx_len;
    wasm->ctx = host->ctx;
    return 0;
}

void lsm_ctx_wasm32_to_host(const struct wasm32_lsm_ctx *wasm, struct host64_lsm_ctx *host) {
    host->id = wasm->id;
    host->flags = wasm->flags;
    host->len = wasm->len;
    host->ctx_len = wasm->ctx_len;
    host->ctx = wasm->ctx;
}

int mnt_id_req_host_to_wasm32(const struct host64_mnt_id_req *host, struct wasm32_mnt_id_req *wasm) {
    wasm->size = host->size;
    wasm->mnt_ns_fd = host->mnt_ns_fd;
    wasm->mnt_id = host->mnt_id;
    wasm->param = host->param;
    wasm->mnt_ns_id = host->mnt_ns_id;
    return 0;
}

void mnt_id_req_wasm32_to_host(const struct wasm32_mnt_id_req *wasm, struct host64_mnt_id_req *host) {
    host->size = wasm->size;
    host->mnt_ns_fd = wasm->mnt_ns_fd;
    host->mnt_id = wasm->mnt_id;
    host->param = wasm->param;
    host->mnt_ns_id = wasm->mnt_ns_id;
}

int mount_attr_host_to_wasm32(const struct host64_mount_attr *host, struct wasm32_mount_attr *wasm) {
    wasm->attr_set = host->attr_set;
    wasm->attr_clr = host->attr_clr;
    wasm->propagation = host->propagation;
    wasm->userns_fd = host->userns_fd;
    return 0;
}

void mount_attr_wasm32_to_host(const struct wasm32_mount_attr *wasm, struct host64_mount_attr *host) {
    host->attr_set = wasm->attr_set;
    host->attr_clr = wasm->attr_clr;
    host->propagation = wasm->propagation;
    host->userns_fd = wasm->userns_fd;
}

int mq_attr_host_to_wasm32(const struct host64_mq_attr *host, struct wasm32_mq_attr *wasm) {
    if (host->mq_flags > 2147483647LL || host->mq_flags < -2147483648LL) return -1;
    wasm->mq_flags = (int32_t)host->mq_flags;
    if (host->mq_maxmsg > 2147483647LL || host->mq_maxmsg < -2147483648LL) return -1;
    wasm->mq_maxmsg = (int32_t)host->mq_maxmsg;
    if (host->mq_msgsize > 2147483647LL || host->mq_msgsize < -2147483648LL) return -1;
    wasm->mq_msgsize = (int32_t)host->mq_msgsize;
    if (host->mq_curmsgs > 2147483647LL || host->mq_curmsgs < -2147483648LL) return -1;
    wasm->mq_curmsgs = (int32_t)host->mq_curmsgs;
    return 0;
}

void mq_attr_wasm32_to_host(const struct wasm32_mq_attr *wasm, struct host64_mq_attr *host) {
    host->mq_flags = (int64_t)(int32_t)wasm->mq_flags;
    host->mq_maxmsg = (int64_t)(int32_t)wasm->mq_maxmsg;
    host->mq_msgsize = (int64_t)(int32_t)wasm->mq_msgsize;
    host->mq_curmsgs = (int64_t)(int32_t)wasm->mq_curmsgs;
}

int msgbuf_host_to_wasm32(const struct host64_msgbuf *host, struct wasm32_msgbuf *wasm) {
    if (host->mtype > 2147483647LL || host->mtype < -2147483648LL) return -1;
    wasm->mtype = (int32_t)host->mtype;
    for (int i = 0; i < 1; i++) wasm->mtext[i] = host->mtext[i];
    return 0;
}

void msgbuf_wasm32_to_host(const struct wasm32_msgbuf *wasm, struct host64_msgbuf *host) {
    host->mtype = (int64_t)(int32_t)wasm->mtype;
    for (int i = 0; i < 1; i++) host->mtext[i] = wasm->mtext[i];
}

int msqid_ds_host_to_wasm32(const struct host64_msqid_ds *host, struct wasm32_msqid_ds *wasm, uint8_t *mem_base) {
    if (ipc_perm_host_to_wasm32(&host->msg_perm, &wasm->msg_perm) != 0) return -1;
    wasm->msg_first = host->msg_first ? (uint32_t)((uint8_t *)host->msg_first - mem_base) : 0;
    wasm->msg_last = host->msg_last ? (uint32_t)((uint8_t *)host->msg_last - mem_base) : 0;
    if (host->msg_stime > 2147483647LL || host->msg_stime < -2147483648LL) return -1;
    wasm->msg_stime = (int32_t)host->msg_stime;
    if (host->msg_rtime > 2147483647LL || host->msg_rtime < -2147483648LL) return -1;
    wasm->msg_rtime = (int32_t)host->msg_rtime;
    if (host->msg_ctime > 2147483647LL || host->msg_ctime < -2147483648LL) return -1;
    wasm->msg_ctime = (int32_t)host->msg_ctime;
    if ((uint64_t)host->msg_lcbytes > 4294967295ULL) return -1;
    wasm->msg_lcbytes = (uint32_t)host->msg_lcbytes;
    if ((uint64_t)host->msg_lqbytes > 4294967295ULL) return -1;
    wasm->msg_lqbytes = (uint32_t)host->msg_lqbytes;
    wasm->msg_cbytes = host->msg_cbytes;
    wasm->msg_qnum = host->msg_qnum;
    wasm->msg_qbytes = host->msg_qbytes;
    if (host->msg_lspid > 32767LL || host->msg_lspid < -32768LL) return -1;
    wasm->msg_lspid = (int16_t)host->msg_lspid;
    if (host->msg_lrpid > 32767LL || host->msg_lrpid < -32768LL) return -1;
    wasm->msg_lrpid = (int16_t)host->msg_lrpid;
    return 0;
}

void msqid_ds_wasm32_to_host(const struct wasm32_msqid_ds *wasm, struct host64_msqid_ds *host, uint8_t *mem_base) {
    ipc_perm_wasm32_to_host(&wasm->msg_perm, &host->msg_perm);
    host->msg_first = wasm->msg_first ? (uint64_t)(uintptr_t)(mem_base + wasm->msg_first) : 0;
    host->msg_last = wasm->msg_last ? (uint64_t)(uintptr_t)(mem_base + wasm->msg_last) : 0;
    host->msg_stime = (int64_t)(int32_t)wasm->msg_stime;
    host->msg_rtime = (int64_t)(int32_t)wasm->msg_rtime;
    host->msg_ctime = (int64_t)(int32_t)wasm->msg_ctime;
    host->msg_lcbytes = wasm->msg_lcbytes;
    host->msg_lqbytes = wasm->msg_lqbytes;
    host->msg_cbytes = wasm->msg_cbytes;
    host->msg_qnum = wasm->msg_qnum;
    host->msg_qbytes = wasm->msg_qbytes;
    host->msg_lspid = wasm->msg_lspid;
    host->msg_lrpid = wasm->msg_lrpid;
}

int new_utsname_host_to_wasm32(const struct host64_new_utsname *host, struct wasm32_new_utsname *wasm) {
    for (int i = 0; i < 65; i++) wasm->sysname[i] = host->sysname[i];
    for (int i = 0; i < 65; i++) wasm->nodename[i] = host->nodename[i];
    for (int i = 0; i < 65; i++) wasm->release[i] = host->release[i];
    for (int i = 0; i < 65; i++) wasm->version[i] = host->version[i];
    for (int i = 0; i < 65; i++) wasm->machine[i] = host->machine[i];
    for (int i = 0; i < 65; i++) wasm->domainname[i] = host->domainname[i];
    return 0;
}

void new_utsname_wasm32_to_host(const struct wasm32_new_utsname *wasm, struct host64_new_utsname *host) {
    for (int i = 0; i < 65; i++) host->sysname[i] = wasm->sysname[i];
    for (int i = 0; i < 65; i++) host->nodename[i] = wasm->nodename[i];
    for (int i = 0; i < 65; i++) host->release[i] = wasm->release[i];
    for (int i = 0; i < 65; i++) host->version[i] = wasm->version[i];
    for (int i = 0; i < 65; i++) host->machine[i] = wasm->machine[i];
    for (int i = 0; i < 65; i++) host->domainname[i] = wasm->domainname[i];
}

int old_utsname_host_to_wasm32(const struct host64_old_utsname *host, struct wasm32_old_utsname *wasm) {
    for (int i = 0; i < 65; i++) wasm->sysname[i] = host->sysname[i];
    for (int i = 0; i < 65; i++) wasm->nodename[i] = host->nodename[i];
    for (int i = 0; i < 65; i++) wasm->release[i] = host->release[i];
    for (int i = 0; i < 65; i++) wasm->version[i] = host->version[i];
    for (int i = 0; i < 65; i++) wasm->machine[i] = host->machine[i];
    return 0;
}

void old_utsname_wasm32_to_host(const struct wasm32_old_utsname *wasm, struct host64_old_utsname *host) {
    for (int i = 0; i < 65; i++) host->sysname[i] = wasm->sysname[i];
    for (int i = 0; i < 65; i++) host->nodename[i] = wasm->nodename[i];
    for (int i = 0; i < 65; i++) host->release[i] = wasm->release[i];
    for (int i = 0; i < 65; i++) host->version[i] = wasm->version[i];
    for (int i = 0; i < 65; i++) host->machine[i] = wasm->machine[i];
}

int oldold_utsname_host_to_wasm32(const struct host64_oldold_utsname *host, struct wasm32_oldold_utsname *wasm) {
    for (int i = 0; i < 9; i++) wasm->sysname[i] = host->sysname[i];
    for (int i = 0; i < 9; i++) wasm->nodename[i] = host->nodename[i];
    for (int i = 0; i < 9; i++) wasm->release[i] = host->release[i];
    for (int i = 0; i < 9; i++) wasm->version[i] = host->version[i];
    for (int i = 0; i < 9; i++) wasm->machine[i] = host->machine[i];
    return 0;
}

void oldold_utsname_wasm32_to_host(const struct wasm32_oldold_utsname *wasm, struct host64_oldold_utsname *host) {
    for (int i = 0; i < 9; i++) host->sysname[i] = wasm->sysname[i];
    for (int i = 0; i < 9; i++) host->nodename[i] = wasm->nodename[i];
    for (int i = 0; i < 9; i++) host->release[i] = wasm->release[i];
    for (int i = 0; i < 9; i++) host->version[i] = wasm->version[i];
    for (int i = 0; i < 9; i++) host->machine[i] = wasm->machine[i];
}

int open_how_host_to_wasm32(const struct host64_open_how *host, struct wasm32_open_how *wasm) {
    wasm->flags = host->flags;
    wasm->mode = host->mode;
    wasm->resolve = host->resolve;
    return 0;
}

void open_how_wasm32_to_host(const struct wasm32_open_how *wasm, struct host64_open_how *host) {
    host->flags = wasm->flags;
    host->mode = wasm->mode;
    host->resolve = wasm->resolve;
}

int perf_event_attr_host_to_wasm32(const struct host64_perf_event_attr *host, struct wasm32_perf_event_attr *wasm) {
    wasm->type = host->type;
    wasm->size = host->size;
    wasm->config = host->config;
    wasm->sample_type = host->sample_type;
    wasm->read_format = host->read_format;
    wasm->disabled = host->disabled;
    wasm->inherit = host->inherit;
    wasm->pinned = host->pinned;
    wasm->exclusive = host->exclusive;
    wasm->exclude_user = host->exclude_user;
    wasm->exclude_kernel = host->exclude_kernel;
    wasm->exclude_hv = host->exclude_hv;
    wasm->exclude_idle = host->exclude_idle;
    wasm->mmap = host->mmap;
    wasm->comm = host->comm;
    wasm->freq = host->freq;
    wasm->inherit_stat = host->inherit_stat;
    wasm->enable_on_exec = host->enable_on_exec;
    wasm->task = host->task;
    wasm->watermark = host->watermark;
    wasm->precise_ip = host->precise_ip;
    wasm->mmap_data = host->mmap_data;
    wasm->sample_id_all = host->sample_id_all;
    wasm->exclude_host = host->exclude_host;
    wasm->exclude_guest = host->exclude_guest;
    wasm->exclude_callchain_kernel = host->exclude_callchain_kernel;
    wasm->exclude_callchain_user = host->exclude_callchain_user;
    wasm->mmap2 = host->mmap2;
    wasm->comm_exec = host->comm_exec;
    wasm->use_clockid = host->use_clockid;
    wasm->context_switch = host->context_switch;
    wasm->write_backward = host->write_backward;
    wasm->namespaces = host->namespaces;
    wasm->ksymbol = host->ksymbol;
    wasm->bpf_event = host->bpf_event;
    wasm->aux_output = host->aux_output;
    wasm->cgroup = host->cgroup;
    wasm->text_poke = host->text_poke;
    wasm->build_id = host->build_id;
    wasm->inherit_thread = host->inherit_thread;
    wasm->remove_on_exec = host->remove_on_exec;
    wasm->sigtrap = host->sigtrap;
    wasm->defer_callchain = host->defer_callchain;
    wasm->defer_output = host->defer_output;
    wasm->bp_type = host->bp_type;
    wasm->branch_sample_type = host->branch_sample_type;
    wasm->sample_regs_user = host->sample_regs_user;
    wasm->sample_stack_user = host->sample_stack_user;
    wasm->clockid = host->clockid;
    wasm->sample_regs_intr = host->sample_regs_intr;
    wasm->aux_watermark = host->aux_watermark;
    wasm->sample_max_stack = host->sample_max_stack;
    wasm->aux_sample_size = host->aux_sample_size;
    wasm->sig_data = host->sig_data;
    wasm->config3 = host->config3;
    wasm->config4 = host->config4;
    return 0;
}

void perf_event_attr_wasm32_to_host(const struct wasm32_perf_event_attr *wasm, struct host64_perf_event_attr *host) {
    host->type = wasm->type;
    host->size = wasm->size;
    host->config = wasm->config;
    host->sample_type = wasm->sample_type;
    host->read_format = wasm->read_format;
    host->disabled = wasm->disabled;
    host->inherit = wasm->inherit;
    host->pinned = wasm->pinned;
    host->exclusive = wasm->exclusive;
    host->exclude_user = wasm->exclude_user;
    host->exclude_kernel = wasm->exclude_kernel;
    host->exclude_hv = wasm->exclude_hv;
    host->exclude_idle = wasm->exclude_idle;
    host->mmap = wasm->mmap;
    host->comm = wasm->comm;
    host->freq = wasm->freq;
    host->inherit_stat = wasm->inherit_stat;
    host->enable_on_exec = wasm->enable_on_exec;
    host->task = wasm->task;
    host->watermark = wasm->watermark;
    host->precise_ip = wasm->precise_ip;
    host->mmap_data = wasm->mmap_data;
    host->sample_id_all = wasm->sample_id_all;
    host->exclude_host = wasm->exclude_host;
    host->exclude_guest = wasm->exclude_guest;
    host->exclude_callchain_kernel = wasm->exclude_callchain_kernel;
    host->exclude_callchain_user = wasm->exclude_callchain_user;
    host->mmap2 = wasm->mmap2;
    host->comm_exec = wasm->comm_exec;
    host->use_clockid = wasm->use_clockid;
    host->context_switch = wasm->context_switch;
    host->write_backward = wasm->write_backward;
    host->namespaces = wasm->namespaces;
    host->ksymbol = wasm->ksymbol;
    host->bpf_event = wasm->bpf_event;
    host->aux_output = wasm->aux_output;
    host->cgroup = wasm->cgroup;
    host->text_poke = wasm->text_poke;
    host->build_id = wasm->build_id;
    host->inherit_thread = wasm->inherit_thread;
    host->remove_on_exec = wasm->remove_on_exec;
    host->sigtrap = wasm->sigtrap;
    host->defer_callchain = wasm->defer_callchain;
    host->defer_output = wasm->defer_output;
    host->bp_type = wasm->bp_type;
    host->branch_sample_type = wasm->branch_sample_type;
    host->sample_regs_user = wasm->sample_regs_user;
    host->sample_stack_user = wasm->sample_stack_user;
    host->clockid = wasm->clockid;
    host->sample_regs_intr = wasm->sample_regs_intr;
    host->aux_watermark = wasm->aux_watermark;
    host->sample_max_stack = wasm->sample_max_stack;
    host->aux_sample_size = wasm->aux_sample_size;
    host->sig_data = wasm->sig_data;
    host->config3 = wasm->config3;
    host->config4 = wasm->config4;
}

int pollfd_host_to_wasm32(const struct host64_pollfd *host, struct wasm32_pollfd *wasm) {
    wasm->fd = host->fd;
    wasm->events = host->events;
    wasm->revents = host->revents;
    return 0;
}

void pollfd_wasm32_to_host(const struct wasm32_pollfd *wasm, struct host64_pollfd *host) {
    host->fd = wasm->fd;
    host->events = wasm->events;
    host->revents = wasm->revents;
}

int rlimit_host_to_wasm32(const struct host64_rlimit *host, struct wasm32_rlimit *wasm) {
    if ((uint64_t)host->rlim_cur > 4294967295ULL) return -1;
    wasm->rlim_cur = (uint32_t)host->rlim_cur;
    if ((uint64_t)host->rlim_max > 4294967295ULL) return -1;
    wasm->rlim_max = (uint32_t)host->rlim_max;
    return 0;
}

void rlimit_wasm32_to_host(const struct wasm32_rlimit *wasm, struct host64_rlimit *host) {
    host->rlim_cur = wasm->rlim_cur;
    host->rlim_max = wasm->rlim_max;
}

int rlimit64_host_to_wasm32(const struct host64_rlimit64 *host, struct wasm32_rlimit64 *wasm) {
    wasm->rlim_cur = host->rlim_cur;
    wasm->rlim_max = host->rlim_max;
    return 0;
}

void rlimit64_wasm32_to_host(const struct wasm32_rlimit64 *wasm, struct host64_rlimit64 *host) {
    host->rlim_cur = wasm->rlim_cur;
    host->rlim_max = wasm->rlim_max;
}

int robust_list_host_to_wasm32(const struct host64_robust_list *host, struct wasm32_robust_list *wasm, uint8_t *mem_base) {
    wasm->next = host->next ? (uint32_t)((uint8_t *)host->next - mem_base) : 0;
    return 0;
}

void robust_list_wasm32_to_host(const struct wasm32_robust_list *wasm, struct host64_robust_list *host, uint8_t *mem_base) {
    host->next = wasm->next ? (uint64_t)(uintptr_t)(mem_base + wasm->next) : 0;
}

int robust_list_head_host_to_wasm32(const struct host64_robust_list_head *host, struct wasm32_robust_list_head *wasm, uint8_t *mem_base) {
    if (robust_list_host_to_wasm32(&host->list, &wasm->list, mem_base) != 0) return -1;
    if (host->futex_offset > 2147483647LL || host->futex_offset < -2147483648LL) return -1;
    wasm->futex_offset = (int32_t)host->futex_offset;
    wasm->list_op_pending = host->list_op_pending ? (uint32_t)((uint8_t *)host->list_op_pending - mem_base) : 0;
    return 0;
}

void robust_list_head_wasm32_to_host(const struct wasm32_robust_list_head *wasm, struct host64_robust_list_head *host, uint8_t *mem_base) {
    robust_list_wasm32_to_host(&wasm->list, &host->list, mem_base);
    host->futex_offset = (int64_t)(int32_t)wasm->futex_offset;
    host->list_op_pending = wasm->list_op_pending ? (uint64_t)(uintptr_t)(mem_base + wasm->list_op_pending) : 0;
}

int rseq_host_to_wasm32(const struct host64_rseq *host, struct wasm32_rseq *wasm) {
    wasm->cpu_id_start = host->cpu_id_start;
    wasm->cpu_id = host->cpu_id;
    wasm->rseq_cs = host->rseq_cs;
    wasm->flags = host->flags;
    wasm->node_id = host->node_id;
    wasm->mm_cid = host->mm_cid;
    wasm->end = host->end;
    return 0;
}

void rseq_wasm32_to_host(const struct wasm32_rseq *wasm, struct host64_rseq *host) {
    host->cpu_id_start = wasm->cpu_id_start;
    host->cpu_id = wasm->cpu_id;
    host->rseq_cs = wasm->rseq_cs;
    host->flags = wasm->flags;
    host->node_id = wasm->node_id;
    host->mm_cid = wasm->mm_cid;
    host->end = wasm->end;
}

int rusage_host_to_wasm32(const struct host64_rusage *host, struct wasm32_rusage *wasm) {
    if (__kernel_old_timeval_host_to_wasm32(&host->ru_utime, &wasm->ru_utime) != 0) return -1;
    if (__kernel_old_timeval_host_to_wasm32(&host->ru_stime, &wasm->ru_stime) != 0) return -1;
    if (host->ru_maxrss > 2147483647LL || host->ru_maxrss < -2147483648LL) return -1;
    wasm->ru_maxrss = (int32_t)host->ru_maxrss;
    if (host->ru_ixrss > 2147483647LL || host->ru_ixrss < -2147483648LL) return -1;
    wasm->ru_ixrss = (int32_t)host->ru_ixrss;
    if (host->ru_idrss > 2147483647LL || host->ru_idrss < -2147483648LL) return -1;
    wasm->ru_idrss = (int32_t)host->ru_idrss;
    if (host->ru_isrss > 2147483647LL || host->ru_isrss < -2147483648LL) return -1;
    wasm->ru_isrss = (int32_t)host->ru_isrss;
    if (host->ru_minflt > 2147483647LL || host->ru_minflt < -2147483648LL) return -1;
    wasm->ru_minflt = (int32_t)host->ru_minflt;
    if (host->ru_majflt > 2147483647LL || host->ru_majflt < -2147483648LL) return -1;
    wasm->ru_majflt = (int32_t)host->ru_majflt;
    if (host->ru_nswap > 2147483647LL || host->ru_nswap < -2147483648LL) return -1;
    wasm->ru_nswap = (int32_t)host->ru_nswap;
    if (host->ru_inblock > 2147483647LL || host->ru_inblock < -2147483648LL) return -1;
    wasm->ru_inblock = (int32_t)host->ru_inblock;
    if (host->ru_oublock > 2147483647LL || host->ru_oublock < -2147483648LL) return -1;
    wasm->ru_oublock = (int32_t)host->ru_oublock;
    if (host->ru_msgsnd > 2147483647LL || host->ru_msgsnd < -2147483648LL) return -1;
    wasm->ru_msgsnd = (int32_t)host->ru_msgsnd;
    if (host->ru_msgrcv > 2147483647LL || host->ru_msgrcv < -2147483648LL) return -1;
    wasm->ru_msgrcv = (int32_t)host->ru_msgrcv;
    if (host->ru_nsignals > 2147483647LL || host->ru_nsignals < -2147483648LL) return -1;
    wasm->ru_nsignals = (int32_t)host->ru_nsignals;
    if (host->ru_nvcsw > 2147483647LL || host->ru_nvcsw < -2147483648LL) return -1;
    wasm->ru_nvcsw = (int32_t)host->ru_nvcsw;
    if (host->ru_nivcsw > 2147483647LL || host->ru_nivcsw < -2147483648LL) return -1;
    wasm->ru_nivcsw = (int32_t)host->ru_nivcsw;
    return 0;
}

void rusage_wasm32_to_host(const struct wasm32_rusage *wasm, struct host64_rusage *host) {
    __kernel_old_timeval_wasm32_to_host(&wasm->ru_utime, &host->ru_utime);
    __kernel_old_timeval_wasm32_to_host(&wasm->ru_stime, &host->ru_stime);
    host->ru_maxrss = (int64_t)(int32_t)wasm->ru_maxrss;
    host->ru_ixrss = (int64_t)(int32_t)wasm->ru_ixrss;
    host->ru_idrss = (int64_t)(int32_t)wasm->ru_idrss;
    host->ru_isrss = (int64_t)(int32_t)wasm->ru_isrss;
    host->ru_minflt = (int64_t)(int32_t)wasm->ru_minflt;
    host->ru_majflt = (int64_t)(int32_t)wasm->ru_majflt;
    host->ru_nswap = (int64_t)(int32_t)wasm->ru_nswap;
    host->ru_inblock = (int64_t)(int32_t)wasm->ru_inblock;
    host->ru_oublock = (int64_t)(int32_t)wasm->ru_oublock;
    host->ru_msgsnd = (int64_t)(int32_t)wasm->ru_msgsnd;
    host->ru_msgrcv = (int64_t)(int32_t)wasm->ru_msgrcv;
    host->ru_nsignals = (int64_t)(int32_t)wasm->ru_nsignals;
    host->ru_nvcsw = (int64_t)(int32_t)wasm->ru_nvcsw;
    host->ru_nivcsw = (int64_t)(int32_t)wasm->ru_nivcsw;
}

int sched_attr_host_to_wasm32(const struct host64_sched_attr *host, struct wasm32_sched_attr *wasm) {
    wasm->size = host->size;
    wasm->sched_policy = host->sched_policy;
    wasm->sched_flags = host->sched_flags;
    wasm->sched_nice = host->sched_nice;
    wasm->sched_priority = host->sched_priority;
    wasm->sched_runtime = host->sched_runtime;
    wasm->sched_deadline = host->sched_deadline;
    wasm->sched_period = host->sched_period;
    wasm->sched_util_min = host->sched_util_min;
    wasm->sched_util_max = host->sched_util_max;
    return 0;
}

void sched_attr_wasm32_to_host(const struct wasm32_sched_attr *wasm, struct host64_sched_attr *host) {
    host->size = wasm->size;
    host->sched_policy = wasm->sched_policy;
    host->sched_flags = wasm->sched_flags;
    host->sched_nice = wasm->sched_nice;
    host->sched_priority = wasm->sched_priority;
    host->sched_runtime = wasm->sched_runtime;
    host->sched_deadline = wasm->sched_deadline;
    host->sched_period = wasm->sched_period;
    host->sched_util_min = wasm->sched_util_min;
    host->sched_util_max = wasm->sched_util_max;
}

int sembuf_host_to_wasm32(const struct host64_sembuf *host, struct wasm32_sembuf *wasm) {
    wasm->sem_num = host->sem_num;
    wasm->sem_op = host->sem_op;
    wasm->sem_flg = host->sem_flg;
    return 0;
}

void sembuf_wasm32_to_host(const struct wasm32_sembuf *wasm, struct host64_sembuf *host) {
    host->sem_num = wasm->sem_num;
    host->sem_op = wasm->sem_op;
    host->sem_flg = wasm->sem_flg;
}

int shmid_ds_host_to_wasm32(const struct host64_shmid_ds *host, struct wasm32_shmid_ds *wasm, uint8_t *mem_base) {
    if (ipc_perm_host_to_wasm32(&host->shm_perm, &wasm->shm_perm) != 0) return -1;
    wasm->shm_segsz = host->shm_segsz;
    if (host->shm_atime > 2147483647LL || host->shm_atime < -2147483648LL) return -1;
    wasm->shm_atime = (int32_t)host->shm_atime;
    if (host->shm_dtime > 2147483647LL || host->shm_dtime < -2147483648LL) return -1;
    wasm->shm_dtime = (int32_t)host->shm_dtime;
    if (host->shm_ctime > 2147483647LL || host->shm_ctime < -2147483648LL) return -1;
    wasm->shm_ctime = (int32_t)host->shm_ctime;
    if (host->shm_cpid > 32767LL || host->shm_cpid < -32768LL) return -1;
    wasm->shm_cpid = (int16_t)host->shm_cpid;
    if (host->shm_lpid > 32767LL || host->shm_lpid < -32768LL) return -1;
    wasm->shm_lpid = (int16_t)host->shm_lpid;
    wasm->shm_nattch = host->shm_nattch;
    wasm->shm_unused = host->shm_unused;
    wasm->shm_unused2 = host->shm_unused2 ? (uint32_t)((uint8_t *)host->shm_unused2 - mem_base) : 0;
    wasm->shm_unused3 = host->shm_unused3 ? (uint32_t)((uint8_t *)host->shm_unused3 - mem_base) : 0;
    return 0;
}

void shmid_ds_wasm32_to_host(const struct wasm32_shmid_ds *wasm, struct host64_shmid_ds *host, uint8_t *mem_base) {
    ipc_perm_wasm32_to_host(&wasm->shm_perm, &host->shm_perm);
    host->shm_segsz = wasm->shm_segsz;
    host->shm_atime = (int64_t)(int32_t)wasm->shm_atime;
    host->shm_dtime = (int64_t)(int32_t)wasm->shm_dtime;
    host->shm_ctime = (int64_t)(int32_t)wasm->shm_ctime;
    host->shm_cpid = wasm->shm_cpid;
    host->shm_lpid = wasm->shm_lpid;
    host->shm_nattch = wasm->shm_nattch;
    host->shm_unused = wasm->shm_unused;
    host->shm_unused2 = wasm->shm_unused2 ? (uint64_t)(uintptr_t)(mem_base + wasm->shm_unused2) : 0;
    host->shm_unused3 = wasm->shm_unused3 ? (uint64_t)(uintptr_t)(mem_base + wasm->shm_unused3) : 0;
}

int sigaction_host_to_wasm32(const struct host64_sigaction *host, struct wasm32_sigaction *wasm, uint8_t *mem_base) {
    // sa_mask: type mismatch (struct vs builtin), byte copy
    for (unsigned i = 0; i < 8; i++) ((unsigned char*)&wasm->sa_mask)[i] = ((unsigned char*)&host->sa_mask)[i];
    if ((uint64_t)host->sa_flags > 4294967295ULL) return -1;
    wasm->sa_flags = (uint32_t)host->sa_flags;
    wasm->sa_restorer = host->sa_restorer ? (uint32_t)((uint8_t *)host->sa_restorer - mem_base) : 0;
    return 0;
}

void sigaction_wasm32_to_host(const struct wasm32_sigaction *wasm, struct host64_sigaction *host, uint8_t *mem_base) {
    for (unsigned i = 0; i < 8; i++) ((unsigned char*)&host->sa_mask)[i] = ((unsigned char*)&wasm->sa_mask)[i];
    host->sa_flags = wasm->sa_flags;
    host->sa_restorer = wasm->sa_restorer ? (uint64_t)(uintptr_t)(mem_base + wasm->sa_restorer) : 0;
}

int sigaltstack_host_to_wasm32(const struct host64_sigaltstack *host, struct wasm32_sigaltstack *wasm, uint8_t *mem_base) {
    wasm->ss_sp = host->ss_sp ? (uint32_t)((uint8_t *)host->ss_sp - mem_base) : 0;
    wasm->ss_flags = host->ss_flags;
    if ((uint64_t)host->ss_size > 4294967295ULL) return -1;
    wasm->ss_size = (uint32_t)host->ss_size;
    return 0;
}

void sigaltstack_wasm32_to_host(const struct wasm32_sigaltstack *wasm, struct host64_sigaltstack *host, uint8_t *mem_base) {
    host->ss_sp = wasm->ss_sp ? (uint64_t)(uintptr_t)(mem_base + wasm->ss_sp) : 0;
    host->ss_flags = wasm->ss_flags;
    host->ss_size = wasm->ss_size;
}

int sigevent_host_to_wasm32(const struct host64_sigevent *host, struct wasm32_sigevent *wasm) {
    for (unsigned i = 0; i < 4; i++) ((unsigned char*)&wasm->sigev_value)[i] = ((unsigned char*)&host->sigev_value)[i];
    wasm->sigev_signo = host->sigev_signo;
    wasm->sigev_notify = host->sigev_notify;
    for (unsigned i = 0; i < 52; i++) ((unsigned char*)&wasm->_sigev_un)[i] = ((unsigned char*)&host->_sigev_un)[i];
    return 0;
}

void sigevent_wasm32_to_host(const struct wasm32_sigevent *wasm, struct host64_sigevent *host) {
    for (unsigned i = 0; i < 4; i++) ((unsigned char*)&host->sigev_value)[i] = ((unsigned char*)&wasm->sigev_value)[i];
    host->sigev_signo = wasm->sigev_signo;
    host->sigev_notify = wasm->sigev_notify;
    for (unsigned i = 0; i < 52; i++) ((unsigned char*)&host->_sigev_un)[i] = ((unsigned char*)&wasm->_sigev_un)[i];
}

int siginfo_host_to_wasm32(const struct host64_siginfo *host, struct wasm32_siginfo *wasm) {
    return 0;
}

void siginfo_wasm32_to_host(const struct wasm32_siginfo *wasm, struct host64_siginfo *host) {
}

int sockaddr_host_to_wasm32(const struct host64_sockaddr *host, struct wasm32_sockaddr *wasm) {
    return 0;
}

void sockaddr_wasm32_to_host(const struct wasm32_sockaddr *wasm, struct host64_sockaddr *host) {
}

int stat_host_to_wasm32(const struct host64_stat *host, struct wasm32_stat *wasm) {
    if ((uint64_t)host->st_dev > 4294967295ULL) return -1;
    wasm->st_dev = (uint32_t)host->st_dev;
    if ((uint64_t)host->st_ino > 4294967295ULL) return -1;
    wasm->st_ino = (uint32_t)host->st_ino;
    if ((uint64_t)host->st_mode > 65535ULL) return -1;
    wasm->st_mode = (uint16_t)host->st_mode;
    if ((uint64_t)host->st_nlink > 65535ULL) return -1;
    wasm->st_nlink = (uint16_t)host->st_nlink;
    if ((uint64_t)host->st_uid > 65535ULL) return -1;
    wasm->st_uid = (uint16_t)host->st_uid;
    if ((uint64_t)host->st_gid > 65535ULL) return -1;
    wasm->st_gid = (uint16_t)host->st_gid;
    if ((uint64_t)host->st_rdev > 4294967295ULL) return -1;
    wasm->st_rdev = (uint32_t)host->st_rdev;
    if (host->st_size > 2147483647LL || host->st_size < -2147483648LL) return -1;
    wasm->st_size = (int32_t)host->st_size;
    if (host->st_blksize > 2147483647LL || host->st_blksize < -2147483648LL) return -1;
    wasm->st_blksize = (int32_t)host->st_blksize;
    if (host->st_blocks > 2147483647LL || host->st_blocks < -2147483648LL) return -1;
    wasm->st_blocks = (int32_t)host->st_blocks;
    if ((uint64_t)host->st_atime > 4294967295ULL) return -1;
    wasm->st_atime = (uint32_t)host->st_atime;
    if ((uint64_t)host->st_atime_nsec > 4294967295ULL) return -1;
    wasm->st_atime_nsec = (uint32_t)host->st_atime_nsec;
    if ((uint64_t)host->st_mtime > 4294967295ULL) return -1;
    wasm->st_mtime = (uint32_t)host->st_mtime;
    if ((uint64_t)host->st_mtime_nsec > 4294967295ULL) return -1;
    wasm->st_mtime_nsec = (uint32_t)host->st_mtime_nsec;
    if ((uint64_t)host->st_ctime > 4294967295ULL) return -1;
    wasm->st_ctime = (uint32_t)host->st_ctime;
    if ((uint64_t)host->st_ctime_nsec > 4294967295ULL) return -1;
    wasm->st_ctime_nsec = (uint32_t)host->st_ctime_nsec;
    return 0;
}

void stat_wasm32_to_host(const struct wasm32_stat *wasm, struct host64_stat *host) {
    host->st_dev = wasm->st_dev;
    host->st_ino = wasm->st_ino;
    host->st_mode = wasm->st_mode;
    host->st_nlink = wasm->st_nlink;
    host->st_uid = wasm->st_uid;
    host->st_gid = wasm->st_gid;
    host->st_rdev = wasm->st_rdev;
    host->st_size = wasm->st_size;
    host->st_blksize = wasm->st_blksize;
    host->st_blocks = wasm->st_blocks;
    host->st_atime = wasm->st_atime;
    host->st_atime_nsec = wasm->st_atime_nsec;
    host->st_mtime = wasm->st_mtime;
    host->st_mtime_nsec = wasm->st_mtime_nsec;
    host->st_ctime = wasm->st_ctime;
    host->st_ctime_nsec = wasm->st_ctime_nsec;
}

int statfs_host_to_wasm32(const struct host64_statfs *host, struct wasm32_statfs *wasm) {
    if (host->f_type > 2147483647LL || host->f_type < -2147483648LL) return -1;
    wasm->f_type = (int32_t)host->f_type;
    if (host->f_bsize > 2147483647LL || host->f_bsize < -2147483648LL) return -1;
    wasm->f_bsize = (int32_t)host->f_bsize;
    if (host->f_blocks > 2147483647LL || host->f_blocks < -2147483648LL) return -1;
    wasm->f_blocks = (int32_t)host->f_blocks;
    if (host->f_bfree > 2147483647LL || host->f_bfree < -2147483648LL) return -1;
    wasm->f_bfree = (int32_t)host->f_bfree;
    if (host->f_bavail > 2147483647LL || host->f_bavail < -2147483648LL) return -1;
    wasm->f_bavail = (int32_t)host->f_bavail;
    if (host->f_files > 2147483647LL || host->f_files < -2147483648LL) return -1;
    wasm->f_files = (int32_t)host->f_files;
    if (host->f_ffree > 2147483647LL || host->f_ffree < -2147483648LL) return -1;
    wasm->f_ffree = (int32_t)host->f_ffree;
    if (__kernel_fsid_t_host_to_wasm32(&host->f_fsid, &wasm->f_fsid) != 0) return -1;
    if (host->f_namelen > 2147483647LL || host->f_namelen < -2147483648LL) return -1;
    wasm->f_namelen = (int32_t)host->f_namelen;
    if (host->f_frsize > 2147483647LL || host->f_frsize < -2147483648LL) return -1;
    wasm->f_frsize = (int32_t)host->f_frsize;
    if (host->f_flags > 2147483647LL || host->f_flags < -2147483648LL) return -1;
    wasm->f_flags = (int32_t)host->f_flags;
    for (int i = 0; i < 4; i++) wasm->f_spare[i] = host->f_spare[i];
    return 0;
}

void statfs_wasm32_to_host(const struct wasm32_statfs *wasm, struct host64_statfs *host) {
    host->f_type = wasm->f_type;
    host->f_bsize = wasm->f_bsize;
    host->f_blocks = wasm->f_blocks;
    host->f_bfree = wasm->f_bfree;
    host->f_bavail = wasm->f_bavail;
    host->f_files = wasm->f_files;
    host->f_ffree = wasm->f_ffree;
    __kernel_fsid_t_wasm32_to_host(&wasm->f_fsid, &host->f_fsid);
    host->f_namelen = wasm->f_namelen;
    host->f_frsize = wasm->f_frsize;
    host->f_flags = wasm->f_flags;
    for (int i = 0; i < 4; i++) host->f_spare[i] = wasm->f_spare[i];
}

int statfs64_host_to_wasm32(const struct host64_statfs64 *host, struct wasm32_statfs64 *wasm) {
    if (host->f_type > 2147483647LL || host->f_type < -2147483648LL) return -1;
    wasm->f_type = (int32_t)host->f_type;
    if (host->f_bsize > 2147483647LL || host->f_bsize < -2147483648LL) return -1;
    wasm->f_bsize = (int32_t)host->f_bsize;
    wasm->f_blocks = host->f_blocks;
    wasm->f_bfree = host->f_bfree;
    wasm->f_bavail = host->f_bavail;
    wasm->f_files = host->f_files;
    wasm->f_ffree = host->f_ffree;
    if (__kernel_fsid_t_host_to_wasm32(&host->f_fsid, &wasm->f_fsid) != 0) return -1;
    if (host->f_namelen > 2147483647LL || host->f_namelen < -2147483648LL) return -1;
    wasm->f_namelen = (int32_t)host->f_namelen;
    if (host->f_frsize > 2147483647LL || host->f_frsize < -2147483648LL) return -1;
    wasm->f_frsize = (int32_t)host->f_frsize;
    if (host->f_flags > 2147483647LL || host->f_flags < -2147483648LL) return -1;
    wasm->f_flags = (int32_t)host->f_flags;
    for (int i = 0; i < 4; i++) wasm->f_spare[i] = host->f_spare[i];
    return 0;
}

void statfs64_wasm32_to_host(const struct wasm32_statfs64 *wasm, struct host64_statfs64 *host) {
    host->f_type = wasm->f_type;
    host->f_bsize = wasm->f_bsize;
    host->f_blocks = wasm->f_blocks;
    host->f_bfree = wasm->f_bfree;
    host->f_bavail = wasm->f_bavail;
    host->f_files = wasm->f_files;
    host->f_ffree = wasm->f_ffree;
    __kernel_fsid_t_wasm32_to_host(&wasm->f_fsid, &host->f_fsid);
    host->f_namelen = wasm->f_namelen;
    host->f_frsize = wasm->f_frsize;
    host->f_flags = wasm->f_flags;
    for (int i = 0; i < 4; i++) host->f_spare[i] = wasm->f_spare[i];
}

int statmount_host_to_wasm32(const struct host64_statmount *host, struct wasm32_statmount *wasm) {
    wasm->size = host->size;
    wasm->mnt_opts = host->mnt_opts;
    wasm->mask = host->mask;
    wasm->sb_dev_major = host->sb_dev_major;
    wasm->sb_dev_minor = host->sb_dev_minor;
    wasm->sb_magic = host->sb_magic;
    wasm->sb_flags = host->sb_flags;
    wasm->fs_type = host->fs_type;
    wasm->mnt_id = host->mnt_id;
    wasm->mnt_parent_id = host->mnt_parent_id;
    wasm->mnt_id_old = host->mnt_id_old;
    wasm->mnt_parent_id_old = host->mnt_parent_id_old;
    wasm->mnt_attr = host->mnt_attr;
    wasm->mnt_propagation = host->mnt_propagation;
    wasm->mnt_peer_group = host->mnt_peer_group;
    wasm->mnt_master = host->mnt_master;
    wasm->propagate_from = host->propagate_from;
    wasm->mnt_root = host->mnt_root;
    wasm->mnt_point = host->mnt_point;
    wasm->mnt_ns_id = host->mnt_ns_id;
    wasm->fs_subtype = host->fs_subtype;
    wasm->sb_source = host->sb_source;
    wasm->opt_num = host->opt_num;
    wasm->opt_array = host->opt_array;
    wasm->opt_sec_num = host->opt_sec_num;
    wasm->opt_sec_array = host->opt_sec_array;
    wasm->supported_mask = host->supported_mask;
    wasm->mnt_uidmap_num = host->mnt_uidmap_num;
    wasm->mnt_uidmap = host->mnt_uidmap;
    wasm->mnt_gidmap_num = host->mnt_gidmap_num;
    wasm->mnt_gidmap = host->mnt_gidmap;
    wasm->str = host->str;
    return 0;
}

void statmount_wasm32_to_host(const struct wasm32_statmount *wasm, struct host64_statmount *host) {
    host->size = wasm->size;
    host->mnt_opts = wasm->mnt_opts;
    host->mask = wasm->mask;
    host->sb_dev_major = wasm->sb_dev_major;
    host->sb_dev_minor = wasm->sb_dev_minor;
    host->sb_magic = wasm->sb_magic;
    host->sb_flags = wasm->sb_flags;
    host->fs_type = wasm->fs_type;
    host->mnt_id = wasm->mnt_id;
    host->mnt_parent_id = wasm->mnt_parent_id;
    host->mnt_id_old = wasm->mnt_id_old;
    host->mnt_parent_id_old = wasm->mnt_parent_id_old;
    host->mnt_attr = wasm->mnt_attr;
    host->mnt_propagation = wasm->mnt_propagation;
    host->mnt_peer_group = wasm->mnt_peer_group;
    host->mnt_master = wasm->mnt_master;
    host->propagate_from = wasm->propagate_from;
    host->mnt_root = wasm->mnt_root;
    host->mnt_point = wasm->mnt_point;
    host->mnt_ns_id = wasm->mnt_ns_id;
    host->fs_subtype = wasm->fs_subtype;
    host->sb_source = wasm->sb_source;
    host->opt_num = wasm->opt_num;
    host->opt_array = wasm->opt_array;
    host->opt_sec_num = wasm->opt_sec_num;
    host->opt_sec_array = wasm->opt_sec_array;
    host->supported_mask = wasm->supported_mask;
    host->mnt_uidmap_num = wasm->mnt_uidmap_num;
    host->mnt_uidmap = wasm->mnt_uidmap;
    host->mnt_gidmap_num = wasm->mnt_gidmap_num;
    host->mnt_gidmap = wasm->mnt_gidmap;
    host->str = wasm->str;
}

int statx_host_to_wasm32(const struct host64_statx *host, struct wasm32_statx *wasm) {
    wasm->stx_mask = host->stx_mask;
    wasm->stx_blksize = host->stx_blksize;
    wasm->stx_attributes = host->stx_attributes;
    wasm->stx_nlink = host->stx_nlink;
    wasm->stx_uid = host->stx_uid;
    wasm->stx_gid = host->stx_gid;
    wasm->stx_mode = host->stx_mode;
    wasm->stx_ino = host->stx_ino;
    wasm->stx_size = host->stx_size;
    wasm->stx_blocks = host->stx_blocks;
    wasm->stx_attributes_mask = host->stx_attributes_mask;
    if (statx_timestamp_host_to_wasm32(&host->stx_atime, &wasm->stx_atime) != 0) return -1;
    if (statx_timestamp_host_to_wasm32(&host->stx_btime, &wasm->stx_btime) != 0) return -1;
    if (statx_timestamp_host_to_wasm32(&host->stx_ctime, &wasm->stx_ctime) != 0) return -1;
    if (statx_timestamp_host_to_wasm32(&host->stx_mtime, &wasm->stx_mtime) != 0) return -1;
    wasm->stx_rdev_major = host->stx_rdev_major;
    wasm->stx_rdev_minor = host->stx_rdev_minor;
    wasm->stx_dev_major = host->stx_dev_major;
    wasm->stx_dev_minor = host->stx_dev_minor;
    wasm->stx_mnt_id = host->stx_mnt_id;
    wasm->stx_dio_mem_align = host->stx_dio_mem_align;
    wasm->stx_dio_offset_align = host->stx_dio_offset_align;
    wasm->stx_subvol = host->stx_subvol;
    wasm->stx_atomic_write_unit_min = host->stx_atomic_write_unit_min;
    wasm->stx_atomic_write_unit_max = host->stx_atomic_write_unit_max;
    wasm->stx_atomic_write_segments_max = host->stx_atomic_write_segments_max;
    wasm->stx_dio_read_offset_align = host->stx_dio_read_offset_align;
    wasm->stx_atomic_write_unit_max_opt = host->stx_atomic_write_unit_max_opt;
    return 0;
}

void statx_wasm32_to_host(const struct wasm32_statx *wasm, struct host64_statx *host) {
    host->stx_mask = wasm->stx_mask;
    host->stx_blksize = wasm->stx_blksize;
    host->stx_attributes = wasm->stx_attributes;
    host->stx_nlink = wasm->stx_nlink;
    host->stx_uid = wasm->stx_uid;
    host->stx_gid = wasm->stx_gid;
    host->stx_mode = wasm->stx_mode;
    host->stx_ino = wasm->stx_ino;
    host->stx_size = wasm->stx_size;
    host->stx_blocks = wasm->stx_blocks;
    host->stx_attributes_mask = wasm->stx_attributes_mask;
    statx_timestamp_wasm32_to_host(&wasm->stx_atime, &host->stx_atime);
    statx_timestamp_wasm32_to_host(&wasm->stx_btime, &host->stx_btime);
    statx_timestamp_wasm32_to_host(&wasm->stx_ctime, &host->stx_ctime);
    statx_timestamp_wasm32_to_host(&wasm->stx_mtime, &host->stx_mtime);
    host->stx_rdev_major = wasm->stx_rdev_major;
    host->stx_rdev_minor = wasm->stx_rdev_minor;
    host->stx_dev_major = wasm->stx_dev_major;
    host->stx_dev_minor = wasm->stx_dev_minor;
    host->stx_mnt_id = wasm->stx_mnt_id;
    host->stx_dio_mem_align = wasm->stx_dio_mem_align;
    host->stx_dio_offset_align = wasm->stx_dio_offset_align;
    host->stx_subvol = wasm->stx_subvol;
    host->stx_atomic_write_unit_min = wasm->stx_atomic_write_unit_min;
    host->stx_atomic_write_unit_max = wasm->stx_atomic_write_unit_max;
    host->stx_atomic_write_segments_max = wasm->stx_atomic_write_segments_max;
    host->stx_dio_read_offset_align = wasm->stx_dio_read_offset_align;
    host->stx_atomic_write_unit_max_opt = wasm->stx_atomic_write_unit_max_opt;
}

int statx_timestamp_host_to_wasm32(const struct host64_statx_timestamp *host, struct wasm32_statx_timestamp *wasm) {
    wasm->tv_sec = host->tv_sec;
    wasm->tv_nsec = host->tv_nsec;
    return 0;
}

void statx_timestamp_wasm32_to_host(const struct wasm32_statx_timestamp *wasm, struct host64_statx_timestamp *host) {
    host->tv_sec = wasm->tv_sec;
    host->tv_nsec = wasm->tv_nsec;
}

int sysinfo_host_to_wasm32(const struct host64_sysinfo *host, struct wasm32_sysinfo *wasm) {
    if (host->uptime > 2147483647LL || host->uptime < -2147483648LL) return -1;
    wasm->uptime = (int32_t)host->uptime;
    for (int i = 0; i < 3; i++) wasm->loads[i] = host->loads[i];
    if ((uint64_t)host->totalram > 4294967295ULL) return -1;
    wasm->totalram = (uint32_t)host->totalram;
    if ((uint64_t)host->freeram > 4294967295ULL) return -1;
    wasm->freeram = (uint32_t)host->freeram;
    if ((uint64_t)host->sharedram > 4294967295ULL) return -1;
    wasm->sharedram = (uint32_t)host->sharedram;
    if ((uint64_t)host->bufferram > 4294967295ULL) return -1;
    wasm->bufferram = (uint32_t)host->bufferram;
    if ((uint64_t)host->totalswap > 4294967295ULL) return -1;
    wasm->totalswap = (uint32_t)host->totalswap;
    if ((uint64_t)host->freeswap > 4294967295ULL) return -1;
    wasm->freeswap = (uint32_t)host->freeswap;
    wasm->procs = host->procs;
    wasm->pad = host->pad;
    if ((uint64_t)host->totalhigh > 4294967295ULL) return -1;
    wasm->totalhigh = (uint32_t)host->totalhigh;
    if ((uint64_t)host->freehigh > 4294967295ULL) return -1;
    wasm->freehigh = (uint32_t)host->freehigh;
    wasm->mem_unit = host->mem_unit;
    for (int i = 0; i < 8; i++) wasm->_f[i] = host->_f[i];
    return 0;
}

void sysinfo_wasm32_to_host(const struct wasm32_sysinfo *wasm, struct host64_sysinfo *host) {
    host->uptime = (int64_t)(int32_t)wasm->uptime;
    for (int i = 0; i < 3; i++) host->loads[i] = wasm->loads[i];
    host->totalram = wasm->totalram;
    host->freeram = wasm->freeram;
    host->sharedram = wasm->sharedram;
    host->bufferram = wasm->bufferram;
    host->totalswap = wasm->totalswap;
    host->freeswap = wasm->freeswap;
    host->procs = wasm->procs;
    host->pad = wasm->pad;
    host->totalhigh = wasm->totalhigh;
    host->freehigh = wasm->freehigh;
    host->mem_unit = wasm->mem_unit;
    for (int i = 0; i < 8; i++) host->_f[i] = wasm->_f[i];
}

int timespec_host_to_wasm32(const struct host64_timespec *host, struct wasm32_timespec *wasm) {
    if (host->tv_sec > 2147483647LL || host->tv_sec < -2147483648LL) return -1;
    wasm->tv_sec = (int32_t)host->tv_sec;
    if (host->tv_nsec > 2147483647LL || host->tv_nsec < -2147483648LL) return -1;
    wasm->tv_nsec = (int32_t)host->tv_nsec;
    return 0;
}

void timespec_wasm32_to_host(const struct wasm32_timespec *wasm, struct host64_timespec *host) {
    host->tv_sec = (int64_t)(int32_t)wasm->tv_sec;
    host->tv_nsec = (int64_t)(int32_t)wasm->tv_nsec;
}

int timeval_host_to_wasm32(const struct host64_timeval *host, struct wasm32_timeval *wasm) {
    if (host->tv_sec > 2147483647LL || host->tv_sec < -2147483648LL) return -1;
    wasm->tv_sec = (int32_t)host->tv_sec;
    if (host->tv_usec > 2147483647LL || host->tv_usec < -2147483648LL) return -1;
    wasm->tv_usec = (int32_t)host->tv_usec;
    return 0;
}

void timeval_wasm32_to_host(const struct wasm32_timeval *wasm, struct host64_timeval *host) {
    host->tv_sec = (int64_t)(int32_t)wasm->tv_sec;
    host->tv_usec = (int64_t)(int32_t)wasm->tv_usec;
}

int timex_host_to_wasm32(const struct host64_timex *host, struct wasm32_timex *wasm) {
    wasm->modes = host->modes;
    if (host->offset > 2147483647LL || host->offset < -2147483648LL) return -1;
    wasm->offset = (int32_t)host->offset;
    if (host->freq > 2147483647LL || host->freq < -2147483648LL) return -1;
    wasm->freq = (int32_t)host->freq;
    if (host->maxerror > 2147483647LL || host->maxerror < -2147483648LL) return -1;
    wasm->maxerror = (int32_t)host->maxerror;
    if (host->esterror > 2147483647LL || host->esterror < -2147483648LL) return -1;
    wasm->esterror = (int32_t)host->esterror;
    wasm->status = host->status;
    if (host->constant > 2147483647LL || host->constant < -2147483648LL) return -1;
    wasm->constant = (int32_t)host->constant;
    if (host->precision > 2147483647LL || host->precision < -2147483648LL) return -1;
    wasm->precision = (int32_t)host->precision;
    if (host->tolerance > 2147483647LL || host->tolerance < -2147483648LL) return -1;
    wasm->tolerance = (int32_t)host->tolerance;
    if (timeval_host_to_wasm32(&host->time, &wasm->time) != 0) return -1;
    if (host->tick > 2147483647LL || host->tick < -2147483648LL) return -1;
    wasm->tick = (int32_t)host->tick;
    if (host->ppsfreq > 2147483647LL || host->ppsfreq < -2147483648LL) return -1;
    wasm->ppsfreq = (int32_t)host->ppsfreq;
    if (host->jitter > 2147483647LL || host->jitter < -2147483648LL) return -1;
    wasm->jitter = (int32_t)host->jitter;
    wasm->shift = host->shift;
    if (host->stabil > 2147483647LL || host->stabil < -2147483648LL) return -1;
    wasm->stabil = (int32_t)host->stabil;
    if (host->jitcnt > 2147483647LL || host->jitcnt < -2147483648LL) return -1;
    wasm->jitcnt = (int32_t)host->jitcnt;
    if (host->calcnt > 2147483647LL || host->calcnt < -2147483648LL) return -1;
    wasm->calcnt = (int32_t)host->calcnt;
    if (host->errcnt > 2147483647LL || host->errcnt < -2147483648LL) return -1;
    wasm->errcnt = (int32_t)host->errcnt;
    if (host->stbcnt > 2147483647LL || host->stbcnt < -2147483648LL) return -1;
    wasm->stbcnt = (int32_t)host->stbcnt;
    wasm->tai = host->tai;
    return 0;
}

void timex_wasm32_to_host(const struct wasm32_timex *wasm, struct host64_timex *host) {
    host->modes = wasm->modes;
    host->offset = (int64_t)(int32_t)wasm->offset;
    host->freq = (int64_t)(int32_t)wasm->freq;
    host->maxerror = (int64_t)(int32_t)wasm->maxerror;
    host->esterror = (int64_t)(int32_t)wasm->esterror;
    host->status = wasm->status;
    host->constant = (int64_t)(int32_t)wasm->constant;
    host->precision = (int64_t)(int32_t)wasm->precision;
    host->tolerance = (int64_t)(int32_t)wasm->tolerance;
    timeval_wasm32_to_host(&wasm->time, &host->time);
    host->tick = (int64_t)(int32_t)wasm->tick;
    host->ppsfreq = (int64_t)(int32_t)wasm->ppsfreq;
    host->jitter = (int64_t)(int32_t)wasm->jitter;
    host->shift = wasm->shift;
    host->stabil = (int64_t)(int32_t)wasm->stabil;
    host->jitcnt = (int64_t)(int32_t)wasm->jitcnt;
    host->calcnt = (int64_t)(int32_t)wasm->calcnt;
    host->errcnt = (int64_t)(int32_t)wasm->errcnt;
    host->stbcnt = (int64_t)(int32_t)wasm->stbcnt;
    host->tai = wasm->tai;
}

int timezone_host_to_wasm32(const struct host64_timezone *host, struct wasm32_timezone *wasm) {
    wasm->tz_minuteswest = host->tz_minuteswest;
    wasm->tz_dsttime = host->tz_dsttime;
    return 0;
}

void timezone_wasm32_to_host(const struct wasm32_timezone *wasm, struct host64_timezone *host) {
    host->tz_minuteswest = wasm->tz_minuteswest;
    host->tz_dsttime = wasm->tz_dsttime;
}

int tms_host_to_wasm32(const struct host64_tms *host, struct wasm32_tms *wasm) {
    if (host->tms_utime > 2147483647LL || host->tms_utime < -2147483648LL) return -1;
    wasm->tms_utime = (int32_t)host->tms_utime;
    if (host->tms_stime > 2147483647LL || host->tms_stime < -2147483648LL) return -1;
    wasm->tms_stime = (int32_t)host->tms_stime;
    if (host->tms_cutime > 2147483647LL || host->tms_cutime < -2147483648LL) return -1;
    wasm->tms_cutime = (int32_t)host->tms_cutime;
    if (host->tms_cstime > 2147483647LL || host->tms_cstime < -2147483648LL) return -1;
    wasm->tms_cstime = (int32_t)host->tms_cstime;
    return 0;
}

void tms_wasm32_to_host(const struct wasm32_tms *wasm, struct host64_tms *host) {
    host->tms_utime = (int64_t)(int32_t)wasm->tms_utime;
    host->tms_stime = (int64_t)(int32_t)wasm->tms_stime;
    host->tms_cutime = (int64_t)(int32_t)wasm->tms_cutime;
    host->tms_cstime = (int64_t)(int32_t)wasm->tms_cstime;
}

int utimbuf_host_to_wasm32(const struct host64_utimbuf *host, struct wasm32_utimbuf *wasm) {
    if (host->actime > 2147483647LL || host->actime < -2147483648LL) return -1;
    wasm->actime = (int32_t)host->actime;
    if (host->modtime > 2147483647LL || host->modtime < -2147483648LL) return -1;
    wasm->modtime = (int32_t)host->modtime;
    return 0;
}

void utimbuf_wasm32_to_host(const struct wasm32_utimbuf *wasm, struct host64_utimbuf *host) {
    host->actime = (int64_t)(int32_t)wasm->actime;
    host->modtime = (int64_t)(int32_t)wasm->modtime;
}

int stat64_host_to_wasm32(const struct host64_stat *host, struct wasm32_stat64 *wasm) {
    wasm->st_dev = host->st_dev;
    wasm->st_mode = host->st_mode;
    if ((uint64_t)host->st_nlink > 4294967295ULL) return -1;
    wasm->st_nlink = (uint32_t)host->st_nlink;
    wasm->st_uid = host->st_uid;
    wasm->st_gid = host->st_gid;
    wasm->st_rdev = host->st_rdev;
    wasm->st_size = host->st_size;
    if (host->st_blksize > 2147483647LL || host->st_blksize < -2147483648LL) return -1;
    wasm->st_blksize = (int32_t)host->st_blksize;
    wasm->st_blocks = host->st_blocks;
    if ((uint64_t)host->st_atime > 4294967295ULL) return -1;
    wasm->st_atime = (uint32_t)host->st_atime;
    if ((uint64_t)host->st_atime_nsec > 4294967295ULL) return -1;
    wasm->st_atime_nsec = (uint32_t)host->st_atime_nsec;
    if ((uint64_t)host->st_mtime > 4294967295ULL) return -1;
    wasm->st_mtime = (uint32_t)host->st_mtime;
    if ((uint64_t)host->st_mtime_nsec > 4294967295ULL) return -1;
    wasm->st_mtime_nsec = (uint32_t)host->st_mtime_nsec;
    if ((uint64_t)host->st_ctime > 4294967295ULL) return -1;
    wasm->st_ctime = (uint32_t)host->st_ctime;
    if ((uint64_t)host->st_ctime_nsec > 4294967295ULL) return -1;
    wasm->st_ctime_nsec = (uint32_t)host->st_ctime_nsec;
    wasm->st_ino = host->st_ino;
    return 0;
}

void stat64_wasm32_to_host(const struct wasm32_stat64 *wasm, struct host64_stat *host) {
    host->st_dev = wasm->st_dev;
    host->st_mode = wasm->st_mode;
    host->st_nlink = wasm->st_nlink;
    host->st_uid = wasm->st_uid;
    host->st_gid = wasm->st_gid;
    host->st_rdev = wasm->st_rdev;
    host->st_size = wasm->st_size;
    host->st_blksize = wasm->st_blksize;
    host->st_blocks = wasm->st_blocks;
    host->st_atime = wasm->st_atime;
    host->st_atime_nsec = wasm->st_atime_nsec;
    host->st_mtime = wasm->st_mtime;
    host->st_mtime_nsec = wasm->st_mtime_nsec;
    host->st_ctime = wasm->st_ctime;
    host->st_ctime_nsec = wasm->st_ctime_nsec;
    host->st_ino = wasm->st_ino;
}
