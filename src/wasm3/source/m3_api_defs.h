/* `#warning` is C23; MSVC C11 mode rejects it. Use the portable
 * `#pragma message` instead so the deprecation note shows up across
 * compilers. */
#pragma message("Using m3_api_defs.h is deprecated. Just include wasm3.h")

